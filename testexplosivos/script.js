import { preguntasVigilante } from './data.js';  // Importa preguntasVigilante desde data.js
const totalPreguntas = preguntasVigilante.length;  // Obtiene la cantidad de preguntas
console.log(`Total de preguntas: ${totalPreguntas}`);  // Muestra la cantidad de preguntas en consola